package homework2.codeToAsk;

import homework2.classes.Car;
import homework2.classes.Ship;
import homework2.classes.Truck;

import java.util.ArrayList;

public class Doubts {

   /* public ArrayList<Double> finalCost() {
        ArrayList<Double> newArray = new ArrayList<>();
            for (int j = 0; j < this.getClient().getProduct().size(); j++) {
                double cost = client.get(i).getProduct().get(j).getWeight() * client.get(i).getProduct().get(j).sizeOfProduct();


                if (this.getClient().getIsComercial() == true && this.getClient().getProduct().get(j).getWeight() > 1000) {
                    cost = cost - cost * 0.2;
                    newArray.add(cost);
                    System.out.println("The total cost its: $" + cost + " " + "It has a discount of %20");


                } else if (this.getClient().getIsComercial() == true && this.getClient().getProduct().get(j).getWeight() > 1000) {
                    cost = cost - cost * 0.1;
                    newArray.add(cost);
                    System.out.println("The total cost its: $" + cost + " " + "It has a discount of %10");


                } else {
                    System.out.println("The total cost its: $" + cost + " " + "It has no discount");
                    newArray.add(0.0);

                }
            }

        return newArray;
    }

    public void createTransport() {
        if (this.getKm() < 1000) {
            Car car1 = new Car(1984, "GD3SDQ");
//            this.setStateDelivery("shipped");
            car1.printData();
            for (int i = 0; i < finalCost().size(); i++) {
                double cost = finalCost().get(i);
                double fcost = cost + car1.addExtraCharge();
                car1.messageExtraCharge();
                System.out.println("Final cost its: " + fcost);
            }


        } else if (this.getKm() > 1000 && this.getKm() < 3000) {
            Truck truck1 = new Truck(1988, "HE3GG3");
//            this.setStateDelivery("shipped");
            truck1.printData();
            for (int i = 0; i < finalCost().size(); i++) {
                double cost = finalCost().get(i);
                double fcost = cost + truck1.addExtraCharge();
                truck1.messageExtraCharge();
                System.out.println("Final cost its: " + fcost);
            }
        }
            else{
                Ship ship1 = new Ship(2001, "EEE33B5");
//            this.setStateDelivery("shipped");
                ship1.printData();
                for (int i = 0; i < finalCost().size(); i++) {
                    double cost = finalCost().get(i);
                    double fcost = cost + ship1.addExtraCharge();
                    ship1.messageExtraCharge();
                    System.out.println("Final cost its: " + fcost);
                }
            }
        }


    */
    }